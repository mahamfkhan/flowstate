# flowstate · TODO

---

## persistence

- [x] store thoughts in `localStorage` (`flowstate.braindump.v1`) so they survive page reloads during development
- [ ] thoughts are currently browser-local only — no backend needed yet
- [ ] **future:** push thoughts to a database (e.g. Supabase, Firebase, or a simple REST API) so they sync across devices and sessions
- [ ] **future:** add user accounts / auth so data is tied to a user, not just a browser

---

## brain dump UI

- [ ] 

